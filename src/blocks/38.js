module.exports = (ctx) => {
    // wow is this an empty file space>
    // you should add some code here
    // or even a PR (wowie)
  ctx.fillStyle = '#bedabe'
  ctx.rect(0, 0, 64, 64)
  ctx.fill()
}
